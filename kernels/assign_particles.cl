/* Kernel to assign particles to control volumes. */

/*
Do two passes:
1) All CVs are just an integer number of particles in the CV.
2) Create CV particle ID arrays with length of the number of particles in the arrays and then assign particles.
*/

__kernel void assign_particles(
) {}
